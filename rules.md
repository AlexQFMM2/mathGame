# MathGame Development Rules

本文件是 MathGame 的项目级硬规则。开始修改代码、目录、UI、游戏规则或构建流程前必须先阅读。

规则优先级：用户当前明确指令 > 本文件 > `docs/` 与 `plan/current/` > 历史计划和其他项目的做法。当前代码若与本文件冲突，不得继续扩大冲突；应修正代码，或在当前计划中明确记录迁移范围和停止条件。稳定的架构变化必须同步更新本文件及相关文档。

## 1. 产品边界

- MathGame 是离线优先、无广告的数学小游戏集合；数独是第一个游戏，不把项目永久写死为数独应用。
- 第一版不引入后端、登录、云同步、在线题库、广告、内购、排行榜、Electron 或通用插件运行时。
- Web 是唯一 UI 主线；Android 只通过 Capacitor 包装 Web 产物，不维护第二套页面和玩法代码。
- 新能力优先解决当前明确需求。第二个游戏出现前，不提前建设大型 `GameModule`、通用路由系统或重型状态框架。
- 玩法可以参考同类产品的信息层级与交互结果，但不得复制其名称、素材、配色组合或高度相似的完整视觉皮肤。

## 2. 依赖方向与职责

依赖只能沿以下方向：

```text
packages/game-core
        ↑
packages/<game>-core
        ↑
apps/web
        ↑
apps/mobile
```

- `packages/game-core` 只保存真正跨游戏稳定的协议、游戏目录和通用常量，禁止放数独专用类型或 UI 代码。
- `packages/sudoku-core` 只保存数独领域模型、规则、候选数、求解、唯一解、生成、难度和结构化提示。
- 所有 `*-core` 必须是纯 TypeScript：不得导入 React、DOM、浏览器存储、Capacitor、Electron或其他平台 API。
- `apps/web` 负责页面、组件、交互编排、路由、文案和平台 adapter；算法规则不得散落在 React 组件中。
- `apps/mobile` 只保存 Capacitor 配置、原生工程和构建脚本；不得复制 `apps/web/src` 或维护移动端专属玩法实现。
- 游戏包之间不得互相依赖。确实通用且已经被至少两个游戏需要的能力，才允许下沉到 `game-core`。
- 禁止通过跨项目绝对路径 import Pokémon、Xstack 或其他工作区代码。参考实现必须在确认许可和适用边界后，复制或重写为本项目自己的实现。

## 3. Web 目录规划

新增功能按以下边界组织；不要求为了目录形式创建空文件：

```text
apps/web/src/
├── components/             跨游戏共享的纯 UI / 布局组件
├── features/               日历、设置等跨游戏产品能力及其页面与状态
├── games/
│   └── sudoku/
│       ├── pages/          数独页面，只组合组件和编排页面级 action
│       ├── components/     棋盘、数字键、工具栏、提示等数独 UI
│       ├── state/          会话 reducer、action、view model
│       └── preview/        复杂组件的独立预览与假数据
├── platform/               haptics、storage、lifecycle 等 adapter
├── styles/                 token、reset 和真正全局的样式
├── App.tsx
└── main.tsx
```

- 页面只负责组件组合、页面布局和页面级跳转，不内联棋盘、提示、存档等复杂流程。
- `features/` 只放已经跨越单个游戏边界的产品能力；单一游戏的页面、状态和组件仍放在 `games/<game>/`，不得借此建立泛化业务层。
- 有内部状态、列表、选择、弹层、滚动或多步交互的 UI 必须拆为组件。
- 同一种交互只能有一个组件事实源；不得为两个页面复制一套棋盘、数字键、对话框或 CSS。
- React 组件和类型使用 `PascalCase`，函数与变量使用 `camelCase`，常量使用语义明确的 `UPPER_SNAKE_CASE` 或现有约定；同一领域内不得混用多套命名风格。
- 文件名与主要导出保持一致，例如 `SudokuBoard.tsx`、`SudokuBoard.css`。
- 单个文件出现多个独立职责时立即拆分；不得以“以后会复用”为由创建无实际边界的抽象层。

## 4. 数独领域与状态规则

- 不可变题面、答案、玩家会话和 UI 选中状态必须概念分离。UI 不得把答案当作普通显示状态读取。
- 核心规则函数必须是确定性的纯函数；不得依赖真实时间、随机全局状态、DOM 或存储。
- 需要随机性的生成器必须显式接收随机源或 seed，使题目和测试可复现。
- 对外展示的题目编号必须由核心包严格解析，并能还原生成所需的游戏类型、难度和 seed；页面不得自行拆字符串或伪造可分享编号。
- React 组件不得直接修改棋盘、格子或笔记集合。领域状态通过明确的 action/reducer 或纯状态转换函数更新。
- 普通输入、笔记、擦除、自动清理笔记和撤销必须共享同一套领域规则；不得在不同按钮回调中各写一份判断。
- 撤销必须完整恢复一次 action 的所有副作用，包括自动清理的候选笔记。
- 求解器、唯一解检查和提示引擎不得依赖 UI。解数量检查达到 2 个解后应立即停止继续搜索。
- 提示必须从当前盘面推导并返回结构化 proof；不得读取标准答案后伪造“唯一候选”等解释。
- 持久化数据必须包含 `schemaVersion`。读取失败、版本不支持或数据非法时要显式进入可恢复状态，不得静默拼补脏数据。
- 每日任务必须固化当天实际要求的游戏 ID，未来新增游戏不得追溯改变历史打卡和全勤结果；旧存档迁移由统一 normalize/migrate 函数负责。
- 浏览器存储和 Capacitor 生命周期只能通过 `platform/` adapter 接入，业务组件不得散落 `localStorage` 或平台判断。

## 5. 固定视口与跨端布局

- 所有主要页面首先按 `320 × 640` 逻辑视口设计，再由最外层 `GameViewport` 等比缩放。
- 缩放公式固定为 `min(availableWidth / 320, availableHeight / 640)`，保持比例，不拉伸、不裁切。
- 只有 `GameViewport` 可以负责整体缩放。内部页面和组件禁止再次使用全局 `transform: scale()`，也不使用 `vw`、`vh` 或多套媒体查询重新定义游戏布局。
- 内部布局使用逻辑像素、明确的行高、列宽和受控滚动区域。固定格式元素必须有稳定尺寸，不能依赖内容把主布局撑开。
- 横屏 Web 仍居中显示竖屏画布；Android 正式工程锁定 portrait。
- 状态栏、刘海和手势区由外层可用区域或 safe-area 处理。棋盘、数字键和主要操作不能被系统区域遮挡。
- 常用按钮和数字键的触控目标不得小于 `40 × 40` 逻辑像素；数独棋盘格因 `306 ÷ 9 = 34` 的几何约束是明确例外。
- 页面不得产生浏览器主滚动；需要滚动的内容必须在组件内部使用尺寸受控、样式一致的滚动区域。

## 6. UI 组件与 CSS 归属

- 每个页面、每个非微型组件使用独立 `.tsx` 和同目录 `.css` 文件。
- 页面 CSS 只负责页面骨架、组件摆放和向组件传入公开尺寸变量，不修改组件内部 DOM。
- 组件 CSS 只负责自身布局、尺寸、状态和滚动区域；组件默认尺寸写在组件自己的 CSS 中。
- 全局样式只保存 reset、字体、根节点、设计 token 和 `GameViewport` 外壳。新页面或组件的主要样式不得继续堆入 `styles.css`。
- CSS 变量必须带归属前缀，例如 `--sudoku-board-cell-size`、`--game-toolbar-height`，禁止无作用域的 `--width`、`--color`。
- 页面定制复用组件时，只能覆盖组件公开的 CSS 变量或 props，不得选择组件内部 class 强改尺寸，也不得复制组件 CSS。
- 当前首页与 `GameViewport` 属于早期基座；首次实质修改对应 UI 时，应按本节拆分 CSS，不得继续扩大集中样式文件。

## 7. 视觉与交互规则

- 视觉基线是安静、清晰、适合长时间思考；当前暖色纸张背景和深绿色强调色是方向，不是不可修改的最终皮肤。
- 页面是成品游戏界面，不是调试面板。数据或规则重构时应保留已有布局、反馈、动效和视觉层级，除非任务明确要求重设计。
- 浅色背景使用深色文字，深色背景使用浅色文字；禁用态降低透明度后仍须可读。
- 状态色只表达状态，不作大面积装饰。错误、选中、预设数字、用户输入、同数字、提示目标等不能只靠颜色区分，还需字重、边框、符号或动画等至少一种额外线索。
- 必须覆盖正常、选中、禁用、错误、空状态、加载/恢复、长文本、内容溢出和完成状态。
- 文本不得撑破布局；固定区域使用省略、换行上限或内部滚动，并验证中英文和较大数字。
- 按钮、面板、弹层和键盘不得互相遮挡。弹层必须有明确焦点、关闭路径和背景交互策略。
- 输入不能依赖 hover；数字键支持连续快速点击。笔记模式必须持续可见，不能只通过一次 toast 表达。
- 动画必须服务于状态变化，并遵守 `prefers-reduced-motion`。不得用长动画阻塞连续输入。
- 浏览器文本选择、双击缩放、长按菜单和页面滚动不得干扰棋盘操作，同时保留必要的键盘可访问性与可读的 ARIA 名称。

## 8. 组件预览与假数据

- 棋盘、数字键、工具栏、提示面板、暂停层、结算页等复杂组件必须能脱离真实存档独立预览。
- 预览数据覆盖与组件相关的主要视觉状态，至少包括普通、选中、禁用、错误、空内容、长文本和边界数量。
- 假数据只用于预览和测试，不得进入正式题库、存档或生产流程。
- 开始建设正式数独 UI 时，同时建立开发态组件预览入口和组件登记表；新增或重构复杂组件时同步登记。
- 预览入口不得污染玩家正式流程，生产构建中应隐藏或由明确的开发开关启用。

## 9. 资源与离线规则

- 核心玩法、题目、字体、图片和必要音效必须随应用可用；断网时不得影响启动、继续游戏、提示和结算。
- 禁止把开发机绝对路径、其他项目路径、临时第三方 URL、`blob:` 或 `data:` URL 保存为业务数据。
- 正式运行时不得依赖参考项目或参考网站的图片、脚本、字体和音频。
- 资源引用使用仓库内稳定相对路径或统一 asset resolver；不得在多个组件中各自拼接资源地址。
- 资源缺失时先修复导入或解析链路。只有业务允许缺省时才显示明确空状态，不用看似正常的假图掩盖 404 或坏数据。
- Android 包内资源和 Web 资源必须来自同一事实源，不能为移动端复制一套未经同步的素材目录。

## 10. 计划与文档

- 大功能进入 `plan/current/`，使用 `[ ]` / `[x]` 维护可验证的阶段清单；完成一个阶段就更新，不在最后一次性补记。
- 计划必须写明目标、范围、非目标、依赖边界、状态模型、异常恢复、验收方法和明确的完成标准。
- 未确认的信息标记为候选或待确认，不把猜测写成已完成事实。
- 架构、目录边界、视口或平台策略变化时，同步更新 `README.md`、`docs/architecture.md` 或 `docs/ui-design.md` 中对应的事实源。
- 历史计划只用于理解背景。当前实现完成后，应归档过期计划或明确其状态，禁止让多个“当前方案”同时生效。

## 11. 验证与完成标准

- 新增或修改数独规则时，必须补对应单元测试；至少覆盖合法、非法、边界和不可变性。
- 求解器测试必须覆盖无解、唯一解、多解和提前停止；提示测试必须同时断言结论与 proof 数据。
- UI 变更必须在真实浏览器点击完整用户路径，不能只靠组件渲染、DOM 强制点击或截图宣称完成。
- UI 验收至少覆盖 `320 × 640` 基准、一个常见手机尺寸、横屏居中状态，以及与改动相关的选中、错误、弹层、暂停和恢复状态。
- 涉及触控、安全区、振动、返回键、后台恢复或离线启动时，Web 验收不能代替 Android 真机 smoke。
- 截图是视觉证据，不代替控制台、资源请求、存档恢复和交互结果检查。
- 完成前至少运行：

```bash
pnpm typecheck
pnpm build
pnpm test
```

- “命令退出成功”不等于有测试保障；如果包没有测试脚本或断言数量为零，必须明确说明。
- 不因为类型检查通过或 happy path 成功一次就宣称完成。完成标准是：规则正确、主要交互可用、错误可恢复、布局无裁切、相关文档同步。

## 12. 提交与安全

- 不提交 `node_modules/`、`dist/`、coverage、日志、临时截图、编辑器状态、签名文件、环境文件或本机 Android 配置。
- 不在代码、计划、提交信息或总结中写入密码、Token、私钥、签名凭据或服务器机密。
- 生成 Android 原生工程、引入大型资源、升级主版本依赖或加入新平台前，必须先确认计划和提交边界。
- 功能开发与发布打包分开处理，除非用户明确要求同时发布。
- 提交前检查 `git status --short --ignored`，确认只有本次相关文件进入提交。

## 13. 开工前简化检查

编码前快速确认：

- 这段逻辑属于通用核心、数独核心、Web UI，还是平台 adapter？
- 是否正在把 UI 需求错误地下沉为跨游戏抽象？
- 是否正在创建第二套相同规则、组件或 CSS？
- 是否能用一个纯函数、一个明确 action 或一个小组件表达？
- 是否保留了 `320 × 640` 视口、离线能力和已有视觉层级？
- 最小的单元测试和真实交互 smoke 分别是什么？

如果答案暴露出职责混乱，先修正边界或更新计划，再继续叠加功能。
